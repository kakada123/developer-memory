{
  "name": "developer-memory",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --names api,desktop --prefix-colors cyan,magenta \"npm run dev:api\" \"npm run dev:desktop\"",
    "dev:desktop": "npm run dev --workspace @developer-memory/desktop",
    "dev:api": "npm run dev --workspace @developer-memory/api",
    "build": "npm run build --workspace @developer-memory/shared-types && npm run build --workspace @developer-memory/api && npm run build --workspace @developer-memory/desktop",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @developer-memory/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.0.0"
  }
}
