import assert from'node:assert/strict';import{describe,it}from'node:test';import{GitParserService}from'./git-parser.service';
describe('Git parsing',()=>{const parser=new GitParserService();it('parses machine separated commit metadata, multiline bodies, and files',()=>{const hash='a'.repeat(40),output=`\x1e${hash}\x1faaaaaaaa\x1fDev\x1fdev@example.test\x1f2025-01-01T00:00:00Z\x1fDev\x1fdev@example.test\x1f2025-01-01T00:00:00Z\x1fSubject\x1fBody line one\nBody line two\x1f\x1ftag: v1, main\n2\t1\tsrc/app.ts\n`;const result=parser.parseLog(output)[0]!;assert.equal(result.hash,hash);assert.equal(result.body,'Body line one\nBody line two');assert.equal(result.files[0]?.relativePath,'src/app.ts');assert.deepEqual(result.tagNames,['v1'])});it('detects braced and plain renames',()=>{assert.deepEqual(parser.parseNumstat('1\t1\tsrc/{old => new}.ts'),{relativePath:'src/new.ts',previousPath:'src/old.ts',changeType:'RENAMED',insertions:1,deletions:1});assert.equal(parser.parseNumstat('0\t0\told.ts => new.ts')?.previousPath,'old.ts')})});
