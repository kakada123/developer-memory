import{Injectable}from'@nestjs/common';import{InjectRepository}from'@nestjs/typeorm';import{Repository}from'typeorm';import{DevelopmentSession,SessionStatus}from'./development-session.entity';
@Injectable()export class SessionRepositoryService{constructor(@InjectRepository(DevelopmentSession)readonly repository:Repository<DevelopmentSession>){}active(projectId:string){return this.repository.findOneBy({projectId,status:SessionStatus.ACTIVE})}list(projectId:string){return this.repository.find({where:{projectId},order:{startedAt:'DESC'},take:100})}one(projectId:string,id:string){return this.repository.findOne({where:{id,projectId},relations:{memories:true}})}}
